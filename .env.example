# Copy this file to .env and fill in the values.
# Used by app.py via os.environ.get(...)

# Optional: GitHub Personal Access Token for higher API rate limits (60→5000 req/hr)
GITHUB_TOKEN=

# Flask secret key (change this in production)
FLASK_SECRET=change-me-in-production

# Set to "true" to enable Flask debug mode (never use in production)
FLASK_DEBUG=false

# Server port (default: 5000)
PORT=5000
